build_environment: Ubuntu 14.04
 
before_install:
  - curl -sSL https://get.docker.io/ubuntu/ | sudo sh  
  - sudo docker pull ubuntu:latest

script: 
  - sudo docker run --rm ubuntu:latest echo "hello world" 

notifications:
  email:
    on_success: change
    on_failure: always 
